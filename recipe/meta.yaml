{% set version = "1.23.0.452_gd1585345" %}

package:
  name: crosstool-ng
  version: {{ version }}

source:
  url: https://github.com/continuumio/crosstool-ng/archive/crosstool-ng-{{ version.split("_")[-1] }}.tar.gz
  sha256: 515485a94bbdff75e81036c8789b9fe8f5e9801aa9ab3e00395ebfc63cccae46

build:
  number: 0
  skip: True  # [not linux]
  missing_dso_whitelist:
    - '*'

requirements:
  build:
    # TODO We need some [not bootstrapping] selector or something?
    - {{ compiler('c') }}
    - autoconf
    - automake
    - texinfo
    - bison
    - flex
    - gettext
    - git
    - help2man
    - make
    - texinfo
    - unzip
    - wget
  host:
    - ncurses
    - libtool
  run:
    - ncurses
    - libtool

test:
  commands:
    - ct-ng -h

about:
  home: http://crosstool-ng.org/
  license: GPL-2.0
  license_file:
    - COPYING
    - licenses.d/gpl.txt
    - licenses.d/lgpl.txt
    - licenses.d/by-sa/deed.en
    - licenses.d/by-sa/legalcode
  summary: A versatile (cross-)toolchain generator.
  dev_url: https://github.com/crosstool-ng/crosstool-ng

extra:
  recipe-maintainers:
    - isuruf
